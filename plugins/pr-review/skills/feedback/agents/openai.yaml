interface:
  display_name: "PR Review: Feedback"
  short_description: "Apply comments through both synchronized PR views"
  default_prompt: "$pr-review:feedback"
policy:
  allow_implicit_invocation: false
